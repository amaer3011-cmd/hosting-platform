version: "3.8"

services:
  bot-host:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    container_name: telegram-bot-host
    restart: unless-stopped
    env_file:
      - ../../.env
    environment:
      PORT: "8080"
      DATABASE_PATH: /app/data/hosting.db
      BOTS_DIR: /app/data/uploaded_bots
      ENCRYPTION_KEY: ""
      RATE_LIMIT_MSGS_PER_MINUTE: "10"
    volumes:
      - bot_data:/app/data
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  bot_data:
