# 🔑 إعداد توكن البوت (BOT_TOKEN)

## المشكلة الشائعة
عند رفع بوت يحتاج إلى `BOT_TOKEN`، يجب إضافته بشكل صحيح عبر واجهة البوت.

## ✅ الطريقة الصحيحة لإضافة التوكن

### الخطوة 1: احصل على توكن البوت
1. افتح @BotFather في تليجرام
2. أرسل `/newbot` أو استخدم بوتًا موجودًا
3. انسخ التوكن (يبدو مثل: `123456:ABCxyz...`)

### الخطوة 2: أضف التوكن للبوت المستضاف

#### الطريقة A: بعد رفع البوت (موصى بها)
1. اذهب إلى «بوتاتي» في بوت الاستضافة
2. اضغط على البوت الذي رفعته
3. اختر «🔐 متغيرات البيئة»
4. اضغط «➕ إضافة متغير»
5. أرسل الرسالة التالية:
   ```
   BOT_TOKEN=123456:ABCxyz...
   ```
   (استبدل `123456:ABCxyz...` بالتوكن الحقيقي)

#### الطريقة B: باستخدام الأمر المباشر
```
/setenv BOT_ID BOT_TOKEN 123456:ABCxyz...
```
حيث `BOT_ID` هو رقم البوت من قائمة «بوتاتي»

### الخطوة 3: شغّل البوت
- اضغط زر «▶️ تشغيل»
- البوت سيبدأ الآن بالتوكن الصحيح

## 🤖 التحليل التلقائي بالذكاء الاصطناعي

عند رفع أي بوت، سيقوم النظام بـ:
1. **تحليل الكود** لاكتشاف المتغيرات المطلوبة
2. **عرض تقرير** يوضح:
   - نوع البوت (telegram, discord, etc.)
   - الحزم المطلوبة
   - متغيرات البيئة الضرورية
   - تحذيرات أمنية إن وُجدت
3. **اقتراح إضافة BOT_TOKEN** إذا كان البوت يحتاجه

## ⚠️ ملاحظات هامة

- التوكن يُخزن **مشفّرًا** في قاعدة البيانات
- لا تُشارك التوكن مع أحد
- إذا فشل البوت، تأكد من:
  - صحة التوكن (انسخه بدقة)
  - أن البوت لديه صلاحيات كافية
  - أنك أضفت التوكن قبل التشغيل

## 🛠️ استكشاف الأخطاء

| المشكلة | الحل |
|---------|------|
| البوت لا يستجيب | تأكد من إضافة BOT_TOKEN بشكل صحيح |
| خطأ "Unauthorized" | التوكن غير صحيح، أعد الحصول عليه من @BotFather |
| البوت يتوقف فورًا | راجع السجلات من زر «📜 السجل» |

## 📞 الدعم

إذا واجهت مشكلة، استخدم:
- `/help` لعرض المساعدة
- `/admin` (للمشرفين) للوصول للسجلات
